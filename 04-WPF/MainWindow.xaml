<Window x:Class="_04_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_04_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
			<RowDefinition Height="1*" />
			<RowDefinition Height="5*" />
			<RowDefinition Height="1*" />
		</Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
<<<<<<< Updated upstream
            <ListBox x:Name="BookList" Grid.Column="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate >
				    <DataTemplate>
					    <Grid >
						    <Grid.ColumnDefinitions>
							    <ColumnDefinition Width="5*"/>
							    <ColumnDefinition Width="5*"/>
                                <ColumnDefinition Width="5*"/>
						    </Grid.ColumnDefinitions>
						    <TextBlock Grid.Column="0" Text="{Binding Id}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
						    <TextBlock Grid.Column="1" Text="{Binding Title}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding IsRead}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
					    </Grid>
				    </DataTemplate>
			    </ListBox.ItemTemplate>
		    </ListBox>
            <Grid Grid.Column="1">
            <Grid.RowDefinitions>
			    <RowDefinition Height="1*" />
			    <RowDefinition Height="1*" />
			    <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
			    <RowDefinition Height="1*" />
			    <RowDefinition Height="1*" />
		    </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
				<ColumnDefinition Width="5*"/>
				<ColumnDefinition Width="5*"/>
			</Grid.ColumnDefinitions>
            <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">ID</Label>
            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding ElementName=BookList, Path=SelectedItem.Id}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Label Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center">Title</Label>
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding ElementName=BookList, Path=SelectedItem.Title}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Label Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center">Author</Label>
            <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding ElementName=BookList, Path=SelectedItem.Author}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Label Grid.Column="0" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Center">Year</Label>
            <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding ElementName=BookList, Path=SelectedItem.Year}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Grid.Row="4" Text="{Binding ElementName=BookList, Path=SelectedItem.IsRead}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Label Grid.Column="0" Grid.Row="5" HorizontalAlignment="Left" VerticalAlignment="Center">Format</Label>
            <TextBox Grid.Column="1" Grid.Row="5" Text="{Binding ElementName=BookList, Path=SelectedItem.Format}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
        <Border Grid.Row="0" />
        <Border Grid.Row="1" />
        <Border Grid.Row="2" />
        <Button Grid.Row="0">Button 4</Button>
        <Button Grid.Row="2">Button 6</Button>
=======
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
			        <RowDefinition Height="9*" />
			        <RowDefinition Height="1*" />
		        </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <ListBox x:Name="BookList" Grid.Column="0" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <EventSetter Event="PreviewMouseLeftButtonDown" Handler="showDetails"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
	    			        <DataTemplate>
		    			        <Grid>
			    			        <Grid.ColumnDefinitions>
				    			        <ColumnDefinition Width="5*"/>
					    		        <ColumnDefinition Width="5*"/>
                                        <ColumnDefinition Width="5*"/>
						            </Grid.ColumnDefinitions>
						            <TextBlock Grid.Column="0" Text="{Binding Id}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    						        <TextBlock Grid.Column="1" Text="{Binding Title}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding IsRead}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
		    			        </Grid>
			    	        </DataTemplate>
			            </ListBox.ItemTemplate>
		            </ListBox>
                </Grid>
                <Grid Grid.Row="1">
                    <Button Click="addComponent" HorizontalAlignment="Left" VerticalAlignment="Center">Add</Button>
                </Grid>
            </Grid>
            <Grid x:Name="Details" Grid.Column="1" Visibility="Hidden">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="8*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
    			        <RowDefinition Height="1*" />
	    		        <RowDefinition Height="1*" />
		    	        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*" />
			            <RowDefinition Height="1*" />
			            <RowDefinition Height="1*" />
    		        </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
		    		    <ColumnDefinition Width="5*"/>
			    	    <ColumnDefinition Width="5*"/>
			        </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">ID</Label>
                    <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding ElementName=BookList, Path=SelectedItem.Id}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <Label Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center">Title</Label>
                    <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding ElementName=BookList, Path=SelectedItem.Title, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <Label Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center">Author</Label>
                    <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding ElementName=BookList, Path=SelectedItem.Author, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <Label Grid.Column="0" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Center">Year</Label>
                    <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding ElementName=BookList, Path=SelectedItem.Year, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Grid.Row="4" Text="{Binding ElementName=BookList, Path=SelectedItem.IsRead, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <Label Grid.Column="0" Grid.Row="5" HorizontalAlignment="Left" VerticalAlignment="Center">Format</Label>
                    <TextBox Grid.Column="1" Grid.Row="5" Text="{Binding ElementName=BookList, Path=SelectedItem.Format, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                </Grid>
                <Grid Grid.Row="2">
                    <Button x:Name="Outer" Click="outerButton" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button x:Name="Inner" Click="innerButton" HorizontalAlignment="Right" VerticalAlignment="Center">
                            Delete
                        </Button>
                    </Button>
                </Grid>
            </Grid>
            <Grid x:Name="Add" Grid.Column="1" Visibility="Visible">
                <Grid.RowDefinitions>
			        <RowDefinition Height="1*" />
			        <RowDefinition Height="1*" />
			        <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
			        <RowDefinition Height="1*" />
			        <RowDefinition Height="1*" />
		        </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
				    <ColumnDefinition Width="5*"/>
				    <ColumnDefinition Width="5*"/>
			    </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">ID</Label>
                <TextBox x:Name="ID" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Label Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center">Title</Label>
                <TextBox x:Name="Title" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Label Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center">Author</Label>
                <TextBox x:Name="Author" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Label Grid.Column="0" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Center">Year</Label>
                <TextBox x:Name="Year" Grid.Column="1" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <TextBox x:Name="IsRead" Grid.Column="1" Grid.Row="4" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Label Grid.Column="0" Grid.Row="5" HorizontalAlignment="Left" VerticalAlignment="Center">Format</Label>
                <TextBox x:Name="Format" Grid.Column="1" Grid.Row="5" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
>>>>>>> Stashed changes
    </Grid>
</Window>
